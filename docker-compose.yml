services:
  app:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "127.0.0.1:${PORT:-8080}:8080"
    volumes:
      - ./storage:/app/storage
      - ./anyway.db:/app/anyway.db
      - ./app:/app/app
